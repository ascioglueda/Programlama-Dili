#include <stdio.h>
 
 // enum=belli baslı ifadeleri sıralamamızı sağlar
 enum hafta{pazartesi=1,sali,carsamba,persembe,cuma,cumartesi,pazar};

 int main(){

     printf("%d",persembe);
     return 0;
 }